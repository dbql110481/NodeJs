// (function (exports, require, module, __filename, __dirname) {

const url = 'http://hphk.kr';

const log = (msg) => {
    // Loooooooooooooooooogic
    console.log(`Logging message: ${msg}`);
};

module.exports = log; // 함수를 외부에서 실행할 수 있도록 해줌
// })